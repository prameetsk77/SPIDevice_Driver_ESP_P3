Main.c -> User Space Code.
Ultrasonic_Kernel.c -> Kernel Space Code.

The code is a direct run. 
If no command line arguments are required then safe distance is set to 7cm.
Else the safe distance is set to whatever is given as a input. Only number should be used to run. the user space code 
ex. "./main 15" ->sets the safe distance to 15 cm